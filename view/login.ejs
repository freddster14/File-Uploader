<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/favicon.png">
  <link rel="stylesheet" href="/css/main.css">
  <link rel="stylesheet" href="/css/form.css">
  <title>Log-In</title>
</head>
<body>
  <main>
    <header>
      <h1>
        <img src="/img/icons8-google-drive.svg">
        Nuvio
      </h1>
    </header>
    <div class="content">
      <div class="content-header">
      <h2>Login here</h2>
        <p>Are you a new user? <a href="/sign-up">Create account here</a></p>
      </div>
      <form action="/login" method="post">
        <label for="email">Email<span style="color: red;">*</span></label>
        <input type="email" id="email" name="email" autocomplete="email" value="<%= formData.email || '' %>">
        <p>
          <%- errors.email ? `<img src='/img/alert-circle-svgrepo-com.svg'>${errors.email}` : '' %>
        </p>
        <label for="password">Password<span style="color: red;">*</span></label>
        <input type="password" id="password" name="password" autocomplete="current-password">
        <p>
          <%- errors.password ? `<img src='/img/alert-circle-svgrepo-com.svg'>${errors.password}` : '' %>
        </p>
        <p style="margin-top: 0;">
          <%- errors.general ? `<img src='/img/alert-circle-svgrepo-com.svg'>${errors.general}` : '' %>
        </p>
        <button class="button" type="submit">Log In</button>
      </form>
    </div>
  </main>
</body>
</html>